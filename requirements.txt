# Основные зависимости
yadisk>=1.2.0
watchdog>=2.1.0
ttkbootstrap>=1.10.0

# Системный трей
pystray>=0.19.0
Pillow>=9.0.0

# Безопасность
cryptography>=41.0.0

# Графики и аналитика
matplotlib>=3.7.0
numpy>=1.24.0

# База данных
# (sqlite3 встроен в Python, дополнительные пакеты не требуются)

# Автозапуск (только Windows)
pywin32>=305; sys_platform == 'win32'
winshell>=0.6; sys_platform == 'win32'

# Сборка в EXE
pyinstaller>=5.13.0; extra == 'build'
pyinstaller-hooks-contrib>=2023.0; extra == 'build'

# Разработка и тестирование
pytest>=7.0.0; extra == 'dev'
black>=23.0.0; extra == 'dev'
isort>=5.12.0; extra == 'dev'
mypy>=1.0.0; extra == 'dev'

# Дополнительные утилиты
requests>=2.31.0  # Для будущих улучшений API
python-dateutil>=2.8.0  # Для работы с датами